---
title: 前端面试汇总(持续更新总结)
date: 2021-02-22
categories:
 - 其他
tags:
 - 面试
image: /vuetimg.jpeg
---

## css部分

### 1、盒子的居中方式。(两种以上)
::: tip
1. flex
2. postion
3. postion + transfrom
:::
   

### 2、伪元素与伪类的区别？
::: tip
1. 伪类：不修改DOM的结构，只为DOM增加样式修改其状态
2. 伪元素：创建虚拟DOM，修改DOM结构。
:::

## js部分

### 1、js数据类型有哪些？什么方法是可以相对友好的检测出具体的数据类型的？

::: tip
数据类型：Undefined、Null、Boolean、Number和String  数据类型————Object  Symbol是es6新增
Object.prototype.toString.call()方法是可以相对友好的检测出具体的数据类型的
:::

### 2、重绘与回流
::: tip
重绘与回流就是看是否有DOM的改变，例如显示跟隐藏，css的显示隐藏，DOM一直存在，这是重绘，反之用display:none来显示隐藏，这是会显示隐藏DOM，这就是回流。
重绘不一定回流，回流一定重绘。
:::

### 3、节流于防抖

[防抖节流](../javascript/防抖节流.md)

::: tip
节流:在N秒内只执行第一次的事件
防抖:在N秒内只执行最后一次的事件
:::

实现思路大致如下:

 节流： 创建一个方法，创建一个定时器，定时器里面是具体的执行方法，每次进入方法判断有没有定时器，有就return，不执行，没有就创建

 防抖： 创建一个方法，创建一个定时器，定时器里面是具体的执行方法，每次进入方法判断有没有定时器，有就清除定时器然后在重新创建一个定时器。

## vue部分

### 1、为什么Vue采用异步渲染呢？

::: tip
Vue 是组件级更新，如果不采用异步更新，那么每次更新数据都会对当前组件进行重新渲染，所以为了性能， Vue 会在本轮数据更新后，在异步更新视图。核心思想 nextTick 。
:::


### 2、说说Vue的生命周期吧，什么时候被调用？每个生命周期内部可以做什么？

::: tip
 1. beforeCreate ：实例初始化之后，数据观测之前调用
 2. created：实例创建万之后调用。实例完成：数据观测、属性和方法的运算、 watch/event 事件回调。无 $el .
 3. beforeMount：在挂载之前调用，相关 render 函数首次被调用
 4. mounted：了被新创建的vm.$el替换，并挂载到实例上去之后调用改钩子。
 5. beforeUpdate：数据更新前调用，发生在虚拟DOM重新渲染和打补丁，在这之后会调用改钩子。
 6. updated：由于数据更改导致的虚拟DOM重新渲染和打补丁，在这之后会调用改钩子。
 7. beforeDestroy：实例销毁前调用，实例仍然可用。
 8. destroyed：实例销毁之后调用，调用后，Vue实例指示的所有东西都会解绑，所有事件监听器和所有子实例都会被移除
:::

::: tip
 1. created：实例已经创建完成，因为他是最早触发的，所以可以进行一些数据、资源的请求。
 2. mounted：实例已经挂载完成，可以进行一些DOM操作。
 3. beforeUpdate：可以在这个钩子中进一步的更改状态，不会触发重渲染。
 4. updated：可以执行依赖于DOM的操作，但是要避免更改状态，可能会导致更新无线循环。
 5. destroyed：可以执行一些优化操作，清空计时器，解除绑定事件。

ajax放在哪个生命周期？：一般放在 mounted 中，保证逻辑统一性，因为生命周期是同步执行的， ajax 是异步执行的。单数服务端渲染 ssr 同一放在 created 中，因为服务端渲染不支持 mounted 方法。
什么时候使用beforeDestroy？：当前页面使用 $on ，需要解绑事件。清楚定时器。解除事件绑定， scroll mousemove 。
:::


### 3、父子组件生命周期调用顺序

::: tip
渲染顺序：先父后子，完成顺序：先子后父
更新顺序：父更新导致子更新，子更新完成后父
销毁顺序：先父后子，完成顺序：先子后父
:::

### 4、Vue组件通信

::: tip
props、$emit、$parent、$children、provide、inject、ref、Event Bus、vuex、$attrs、$listeners
:::

### 5、Computed watch
::: tip
computed：默认computed也是一个watcher具备缓存，只有当依赖的数据变化时才会计算, 当数据没有变化时, 它会读取缓存数据。如果一个数据依赖于其他数据，使用 computed
watch：每次都需要执行函数。  watch 更适用于数据变化时的异步操作。如果需要在某个数据变化时做一些事情，使用watch。

:::

### 6、v-if 和 v-show 区别
::: tip
v-if 如果条件不成立不会渲染当前指令所在节点的DOM元素
v-show 只是切换当前DOM的显示与隐藏
:::

### 7、组件中的data为什么是函数
::: tip
避免组件中的数据互相影响。同一个组件被复用多次会创建多个实例，如果 data 是一个对象的话，这些实例用的是同一个构造函数。为了保证组件的
数据独立，要求每个组件都必须通过 data 函数返回一个对象作为组件的状态。
:::

### 8、action 与 mutation 的区别
::: tip
mutation 是同步更新， $watch 严格模式下会报错
action 是异步操作，可以获取数据后调用 mutation 提交最终数据
:::

### 9、你都做过哪些Vue的性能优化？

1. 尽量减少data中的数据，data中的数据都会增加getter和setter，会收集对应的watcher 
2. v-if和v-for不能连用
3. 在更多的情况下，使用v-if替代v-show
4. 用路由懒加载、异步组件
5. 防抖、节流
6. 第三方模块按需导入
7. 长列表滚动到可视区域动态加载
8. 图片懒加载


### 10、hash路由和history路由实现原理说一下
::: tip
location.hash的值实际就是URL中#后面的东西。
history实际采用了HTML5中提供的API来实现，主要有history.pushState()和history.replaceState()。
:::
