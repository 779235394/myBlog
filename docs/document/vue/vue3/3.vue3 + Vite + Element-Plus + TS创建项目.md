---
title: vue3 + TS + Vite + Vuex4 + Element-Plus 创建项目
date: 2021-03-29
categories:
  - VUE
tags:
  - VUE
image: /vuetimg.jpeg
---

## 创建项目

输入命令按步骤往下即可,详细 API 可参考 Vite 官方文档

[vite 官网](https://cn.vitejs.dev/)

```js
npm init @vitejs/app

yarn create @vitejs/app

选择 Vue - TS 版本
```

## 引入 Element-Plus

[Element-Plus 官方文档](https://element-plus.gitee.io/#/zh-CN/component/installation)

```js
npm i element-plus

yarn add element-plus
```

## 引入 Vuex4

[Vuex4  官方文档](https://next.vuex.vuejs.org/)

```js
npm install vuex@next

yarn add vuex@next
```

## 配置修改

main.ts

```js
import { createApp } from "vue";
import App from "./App.vue";
import { store } from "./store";
import "element-plus/lib/theme-chalk/index.css";
import ElementPlus from "element-plus";

const app = createApp(App);
app.use(ElementPlus);
app.use(store);
app.mount("#app");
```

## Vuex 模块化

新建文件

```
--- store
  --- modules  模块化文件
    --- test.ts
  --- index.ts
```

test.ts

```js
export default {
  namespaced: true,
  state: {
    count: 10,
  },
  mutations: {
    SET_COUNT(state: any) {
      state.count++;
    },
  },
  actions: {
    set_count(ctx: any) {
      ctx.commit("SET_COUNT");
    },
  },
};
```

index.ts

```js
import { createStore } from "vuex";
import textModules from "./modules/test";

export const store = createStore({
  modules: {
    textModules,
  },
});
```

## 测试

修改 HelloWorld

```vue
<template>
  <h1>{{ msg }}</h1>
  <h1>count: {{ count }}</h1>
  <el-button type="success" @click="addCount">+1</el-button>
</template>

<script lang="ts">
import { defineComponent, computed } from "vue";
import { useStore } from "vuex";
export default defineComponent({
  name: "HelloWorld",
  props: {
    msg: {
      type: String,
      required: true,
    },
  },
  setup: () => {
    const store = useStore();
    const count = computed(() => {
      return store.state.textModules.count;
    });
    return {
      count,
      addCount: () => store.dispatch("textModules/set_count"),
    };
  },
});
</script>
```

当出现 element 的按钮并能成功 count++，说明配置 ok

## 资源目录

[vite 官网](https://cn.vitejs.dev/)

[Element-Plus 官方文档](https://element-plus.gitee.io/#/zh-CN/component/installation)

[Vue3 官方文档](https://v3.vuejs.org/)

[TypeScript 中文手册](https://typescript.bootcss.com/)

[vue3 + TS + Vite + Vuex4 + Element-Plus 源码](https://github.com/779235394/vue3-next)
